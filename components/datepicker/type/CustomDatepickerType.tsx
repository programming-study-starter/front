export interface CustomDatepickerType {
  onModeChange: Function, 
  pickerDate: string, 
  onPickerDateChange: Function,
  selectedDate: string,
  onSelect: Function,
  minDate: date,
  maxDate: date,
}